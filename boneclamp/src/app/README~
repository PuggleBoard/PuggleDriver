Functionality descriptions of files in this folder.

maps.h - memory maps. File used both by the PRU program and the
userspace program. Thus only defines and includes allowed.

boneclamp.h/c - boneclamp library. Currently only initializes
a data struct pointing towards the shared memory of the PRU.

boneclamp.p - PRU program for data acquisition

acq.c - userspace program that does something with the
acquired data
